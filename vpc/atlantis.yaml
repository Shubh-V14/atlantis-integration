projects:
  - dir: deployment/UAT-Infra/plus
    name: uat_infra
    workflow: uat_infra
    workspace: uat_infra
    autoplan:
      enabled: true

version: 3
workflows:
  uat_infra:
    plan:
      steps:
        - init
        - plan

    apply:
      steps:
        - apply

